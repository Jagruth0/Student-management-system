<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href=<%= locals.classname ? "../../css/styles.css" : "../css/styles.css" %>>
</head>
<body>
    <div class="grid-container">
        <div class="sidebar grid-item d-flex flex-column flex-shrink-0 p-3 text-bg-dark">
            <span class="fs-4">School</span>
          <hr>
          <ul class="nav nav-pills flex-column mb-auto">
            <li class="nav-item">
              <form class="nav-link <%= locals.currentAnnoun ? 'active' : 'link-body-emphasis' %>" aria-current="<%= locals.currentAnnoun ? 'page' : '' %>" action="/teacher/announcements" method="get">
                <button class="btn ps-3" type="submit">
                  Announcements
                </button>
              </form>
            </li>
            <li class="nav-item">
              <form class="nav-link <%= locals.currentAtten ? 'active' : 'link-body-emphasis' %>" aria-current="<%= locals.currentAtten ? 'page' : '' %>" action="/teacher/attendance" method="get">
                <button class="btn ps-3" type="submit">
                  Attendance
                </button>
              </form>
            </li>
            <!-- <li class="nav-item">
              <form class="nav-link <%= locals.currentStudy ? 'active' : 'link-body-emphasis' %>" aria-current="<%= locals.currentStudy ? 'page' : '' %>" action="" method="get">
                <button class="btn ps-3" type="submit">
                  Study Material
                </button>
              </form>
            </li> -->
            <li class="nav-item">
              <form class="nav-link <%= locals.currentRes ? 'active' : 'link-body-emphasis' %>" aria-current="<%= locals.currentRes ? 'page' : '' %>" action="/teacher/results" method="get">
                <button class="btn ps-3" type="submit">
                  Results
                </button>
              </form>
            </li>
          </ul>
          <hr>
        </div>


        <header class="text-bg-dark p-3">
            <div class="d-flex">
                <div class="dropdown">
                    <button class="btn btn-dark dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                      <img src=<%= locals.classname ? "../../assets/user.png" : "../assets/user.png" %> alt="user" width="32" height="32" class="rounded-circle">
                      <span class="p-1"> <% if (locals.username) { %>
                        <%= username %>
                      <% } else {%>
                        UserName
                        <% } %>
                       </span>
                    </button>
                    <ul class="dropdown-menu">
                      <li><a class="dropdown-item" href="">Profile</a></li>
                      <li><hr class="dropdown-divider"></li>
                      <li><a class="dropdown-item" href="/logout/teacher" role="button">Log out</a></li>
                    </ul>
                  </div>
            </div>
        </header>